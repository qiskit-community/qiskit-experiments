---
fixes:
  - |
    Fixed :meth:`.BackendData.coupling_map` and
    :meth:`.BackendData.drive_freqs` raising exceptions when the underlying
    backend has ``None`` for its coupling and qubit property entries.
    Fixed `#1035 <https://github.com/Qiskit/qiskit-experiments/issues/1035>`__
