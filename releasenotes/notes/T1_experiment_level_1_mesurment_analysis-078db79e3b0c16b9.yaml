---
features:
  - |
    Added new class :class:`.T1KerneledAnalysis`. This class is used for T1
    Experiment with the option `meas_level=MeasLevel.KERNELED`. The analysis
    normalize the data and fixing its orientation.
    