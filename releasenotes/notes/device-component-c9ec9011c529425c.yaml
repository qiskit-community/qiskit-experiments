---
developer:
  - |
    Going forward, the ``device_component`` experiment metadata field should be populated in with a
    list of components used in the experiment. See the :doc:`custom experiments tutorial
    </tutorials/custom_experiment>` for more details.

